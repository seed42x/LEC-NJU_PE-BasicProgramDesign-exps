0.
431行注释除可能存在问题
1.
总计算有括号情况下的范围查找忽略的(())的情况？
2.
函数调用的查找需要考量(f(x,y))或者是+f(x,y)的情况，函数名前边有可能是运算符，也有可能是括号
444行起，对所有的表达式求值需要检查
3.
496行，如果为函数调用情况下的代码正确性检查
4.
433行起，对表达式中所有函数求值，此处是否应该考量因为之前已经对所有变量进行了求值，因此只要表达式中有标识符，那么必然会是一个函数调用？？？
5.
函数调用时，是否仅仅只是简简单单用实际参数表去替换形式参数表？如果遇到了实参表中含有复合式子应该怎么办？
6.
是否考量由于在FuncsEval中已经单独调用FuncCall了，将所有的函数求值已经进行了，那么在Calculate中是否可以去除Base case中的FuncCall，否则会导致重复调用，那么同理对于变量Vars也是如此.

单个函数的正则表达式：([a-zA-Z]+[(][a-zA-Z)(+-//*//,]*[)])

运算的正则表达式
函数调用+常量+...+括号的添加？？？
 
[)(]*([a-zA-Z]+|[0-9.]+|([a-zA-Z]*[(][a-zA-Z0-9.+-,//*//)(]*[)])){1}([)()]*[)(]*([+-//*//]{1}[)(]*([a-zA-Z]+|[0-9.]+|([a-zA-Z]+[(][a-zA-Z0-9.)(+-//*//,]*[)]))[)()]*)*)*[)(]*

Var x=x+1
682行，辅助检查输出，记得删除
C++中正则表达式在[]中添加+/-号可能会导致范围扩大而并不是仅仅只添加正负号
正则表达式中正负号需要进行修改

更改记录：
1.添加了正则表达式对综合计算式子的检测				用以提升程序稳定性
2.支持Var x=x+1定义，同时添加了最大递归深度限制		用以提升程序稳定性
3.

Debug记录：
1.Bug 	Var x=x+1递归定义导致程序崩溃																		Done
2.Bug 	函数创建时函数体变量合法检测：修改为只在形式参数表中找(原本在 形式参数表+变量库 中查找会导致冲突)		Done
3.Bug 	无括号综合运算中修正了原本由于starti、endi使用错误导致的程序出错										Done
4.Bug 	正则表达式有误加遗漏(+-正负号匹配应该未转义导致范围扩大以及忽略了负数的匹配)							Done
5.Bug 	二元运算找操作数时到小数点会中断导致小数无法正确二元运算												Done
6.Bug 	函数调用时将函数指令最后一个右括号也加入到了结果返回，导致括号数目不匹配								Done
7.Bug 	在求取了式子中的所有函数和变量之后未重新数括号对数目，导致运算错误									Done
8.Bug 	